apiVersion: operator.victoriametrics.com/v1beta1
kind: VMRule
metadata:
  name: test-alerts
spec:
  groups:
    - name: test
      rules:
        - alert: TestServiceDown
          expr: test_service_up{service="abc"} == 0
          for: 0s
          labels:
            severity: critical
